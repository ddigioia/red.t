<%= form_for @link do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :body %>
<%= f.text_area :body %>

<%= f.label :url %>
<%= f.text_field :url %>

<%= f.submit %>

<% end %>

